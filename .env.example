# This project is safe-by-default. Do not change EXECUTION_MODE until the live keeper has been audited.
EXECUTION_MODE=disabled
PLATFORM_FEE_BPS=500
TARGET_CHAIN=unconfigured
PAYOUT_VAULT_FACTORY=
DATABASE_URL=
REDIS_URL=
WORKER_POLL_INTERVAL_MS=3600000
# Temporary public routing compatibility; Railway provides PORT automatically.
PUBLIC_PORT=3000
